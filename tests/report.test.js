import supertest from "supertest";
import app from '../src/app.js'

describe('Intergration tests - report routes', () => {
    it('should respond with 200 when sending the report manualy', () => {
        console.log("the test should run here")
    })
})